import { createStyles } from '@mantine/core';

export default createStyles((theme) => ({
  root: {
    position: 'fixed',
    marginTop: '75vh',
    zIndex: 100,
    // justifyContent: "center",
    display: 'flex',
  },
}));
